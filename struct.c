#include <stdio.h>

typedef

    struct Person
{
  char name[50];
  int age;
  int weight;
  int height;
}

int
main()
{

  return 0;
}

/* STRUCT
 * it's a custom datatype. (not a class)
 * when to call the struct, we need to use `struct structIdentifier demoName;`
 */

/* TYPEDEF
 * it's needed because of struct. when we call struct we have to use struct all the time.
 *
 */